#include <ctype.h>

	void TaskInit (void);
	void TaskAddTail (char);
	unsigned char TaskGet (void);
	void TaskInsertAt (int pos, char val);
	char task(void);
	unsigned char TaskQuery(int pos);
	int TaskLen (void);
